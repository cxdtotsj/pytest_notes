# 概述

## 安装

1. 运行VENV环境 <br>
    `python -m venv venv`

2. 安装依赖 <br>
    `pip install -r requirements.txt`

3. 在tasks_proj目录下，安装task <br>
    ```
    pip install .
    或者
    pip install -e .
    ```